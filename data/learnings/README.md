# Per-portal learnings

One file per onboarded portal, e.g. `<portal>.md`. The file is created during
onboarding (`portal-onboarding` skill) and collects that portal's specific gotchas.
`[L-nnn]` ids are global and unique across the union of all learnings files (see the
protocol in `data/learnings.md`).
