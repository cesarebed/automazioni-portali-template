{
  "name": "portal-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic multi-portal bot: fills cases on web portals from the company's data source (Playwright)",
  "scripts": {
    "check": "node bot.js --check"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  }
}
